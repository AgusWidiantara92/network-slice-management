import React from 'react';
import LLMProviderClient from '@/components/llm/LLMProviderClient';

export const metadata = {
  title: 'LLM Provider Settings - NSM Controller',
};

export default function LLMProviderPage() {
  return <LLMProviderClient />;
}
